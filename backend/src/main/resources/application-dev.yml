# Dev profile: no MySQL or Docker required (in-memory H2 database)
spring:
  datasource:
    url: jdbc:h2:mem:biopower_db;DB_CLOSE_DELAY=-1;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

mqtt:
  enabled: false

logging:
  level:
    com.biopower: INFO
